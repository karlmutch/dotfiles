if [[ "$OSX" = "1" ]]
then
    if [[ "$TERM" = "rxvt-unicode-256color" ]]
    then
      export TERM=rxvt-256color
    fi
fi

OS=$(uname -s)
case "$OS" in
    Darwin*) ;;
    *) eval $( dircolors -b ~/.ls_colors ) ;;
esac
